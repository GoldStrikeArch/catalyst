Execute the Wave-4 backend slot in /Users/mpl4/Desktop/workspace/OSS/topcoat/spike/rustc-codegen-js per the plan at /Users/mpl4/.claude/plans/alright-look-at-asd-md-parallel-metcalfe.md (Track B §B2 + the banked reactive-for gap). BANKING: build/logs/wave4-backend-report.md from your FIRST action; bank per step + "DYLIB STABLE" checkpoints after each green rebuild (framework agent polls it). You own: backend/, view-abi/, view-dom/src/lower + template/dom_writer (the reactive-for fix spans them — the framework agent is NOT in view-dom/src this wave; they own view-dom-macro only), scripts/, examples/, runtime/shim.js, build/, CONTRACT.md. READ ONLY: contract/ (fake chart-lib fixtures land there in parallel — poll contract/parity/wave4-contract-notes.md), demo-app/ (verify at end), jsc-build/, crates/. No git commit. Skill "style" first. Baseline: test 46/46 (9 modes), emit 20/20, module 19/19, dom 19/19, ABI v4+event markers. Read build/logs/wave3-\*.md first.

## Items

1. **The reactive-for gap (M) — the search island's last blocker.** `for` fills its hole with Fill::ControlFlow so a list renders once and never updates. Design + implement the reactive list path: when the iterated expression (or the row content) reads signals, the hole must re-run — the Child hole's value becomes a closure the backend wraps in the insert's reactive accessor (upstream's insert(parent, () => rows) semantics; read your wave-3 bank's two notes + corpus 13's reference). Non-signal-reading fors stay static (no regression in emitted size for the existing fixtures — measure). Keyed rows must keep node identity across re-runs (the keyed_row machinery exists — extend the identity harness to a reactive keyed re-render). Fixtures: a reactive filtered list (signal-driven), trace + identity asserted; rebaseline anything affected.
2. **B2: #[js_extern] (L) — Stage 4's headline.** Per the plan Track B §B2: the descriptor encoded into the extern decl's link_section (`rcgjs.ext.<compact encoding — design it: postcard+base64 or a terse text form; document>`), emitted by a `js_extern!`/attribute proc macro (put the macro in view-dom-macro? NO — that's framework's. Put it in a NEW spike crate js-extern-macro OR — cleaner — in view-abi as a decl macro? proc-macro needed for attr parsing; create spike crate `js-extern-macro` you own, workspace member). Shapes (Melange's set, scoped to what the dashboard needs + one of each): module import + call (import { Chart } from "chart.js"; Chart(...)), new (new Chart(...)), send/method (chart.update(...)), get/set (obj.prop), index. Per-arg passthrough (the value model already crosses structs/strings/numbers); `#[js(nullable)]` return → null-to-Option. Backend: intercept at abi.rs ahead of Callee::Runtime, decode the descriptor, emit per shape with ItemKind::Import from the declared module specifier. Tests: the contract agent is landing a FAKE chart lib (vendored, trace-recording) + expected vectors — poll their bank and build your fixtures against it (a chart create + update + a getter + nullable return); plus unit tests on the descriptor codec. CONTRACT.md section.
3. **If capacity: the executor (M).** The spawn/waker/microtask shim driving landed coroutines from event handlers (never setup — §14). If capacity is short, bank the precise remaining spec instead.

All suites green; demo-app build + smoke at the end after your last DYLIB STABLE (touch build.rs first — build-script caching). Report (banked + final): reactive-for design, the descriptor encoding + shapes landed, executor status, suite counts, deviations.

---

Execute the Wave-4 framework slice in /Users/mpl4/Desktop/workspace/OSS/topcoat per the plan at /Users/mpl4/.claude/plans/alright-look-at-asd-md-parallel-metcalfe.md. BANKING: spike/rustc-codegen-js/build/logs/wave4-framework-report.md from your FIRST action. You own: demo-app/, view-dom-macro/, crates/topcoat-dom, crates/ additive. READ ONLY: view-dom/src (BACKEND agent's this wave — the reactive-for fix is theirs; poll build/logs/wave4-backend-report.md), view-abi/, backend/, scripts/, contract/, jsc-build/ (read the Chunking API). COMPILE COORDINATION: poll the backend bank's latest "DYLIB STABLE" before each demo-app build; `touch build.rs` first, always (build-script caching bit wave 3). No git commit. Skill "style". Baselines: workspace 1772/1869, view-dom+macro 125, demo-app smoke 19/19 + budgets, parity green. Read build/logs/wave3-\*.md + contract/parity/wave3-contract-notes.md first.

## Items

1. **Search island client half (M) — completes the G2 milestone.** The blockers fell: Event::target_value/prevent_default landed (view-abi, 8/8), and the backend lands reactive-for THIS wave (poll their bank; do the non-for parts first). Your wave-3 design stands: @input handler → Sig<&str>… (target_value returns the value; write it to a String Sig? no-alloc: the debounce extern carries the query string) → extern "C" search_debounce(url, query, ms, sink: Sig) with the reply written to a signal → results rendered via a (now-reactive) for over the parsed reply (your no-serializer &'static-str + iterator design from wave 3 — keep it; adjust if the backend's serde_json-for-typed-structs verdict makes a typed reply nicer: poll their wave-3 bank — serialization works for structs now, f64 excluded → your SearchHit can carry integer scores). The **rt member for search_debounce: shim.js is backend-owned — REQUEST via bank if missing; else the demo's loader/dom.rs can install it app-side (the demo already installs **rt extensions — wave-G1 glue precedent... actually islands have no glue.js; the loader module is yours — install the debounce+fetch member there, honoring contract/fixtures/procedure-wire.json). Acceptance: /island/search serves, hydrates clean, node smoke drives input→debounce→stubbed fetch→list update; then bank "SEARCH ISLAND SERVING" (the contract agent flips its fixture live).
2. **Demo-app goes chunked + lazy (M).** Switch demo-app's build.rs to the Chunking API (islands: counter, nested, search; shared chunk; import map from the generated fragment); serve the chunks; the loader gains the lazy-hydration gate: IntersectionObserver per <topcoat-island>, opt-out via data-tl="eager" (counter eager, nested+search lazy — demonstrates both). Budgets updated to per-chunk (the backend's wave-3 measurements suggest ~20% above attributed numbers; re-measure, band, record). Smoke extended: lazy islands don't import their chunk until visible (jsdom IntersectionObserver stub — assert the import happened only after the trigger).
3. **Dev-mode hydration-loss warning (S).** The parity harness proved a key miss is now SILENT loss of hydration (working client re-render, nothing logs). In the loader (dev builds only — gate on the dev script's presence or an attribute the server writes only when TOPCOAT*DEV_URL is set — pick the cleanest existing signal): after hydrate(), check the island's registry consumption/root identity the cheap way (e.g. compare a marked server node's identity or check *$HY registry leftovers scoped to the renderId prefix) and console.warn naming the island + key on loss. Assert in smoke (positive: no warning; negative: perturbed key → warning fires).
4. **A7 upstreaming step (M, as far as clean):** move the SERVER-side pieces that are now stable into crates/topcoat-dom (the seam module, Result/ResultExt, json seam docs) and wire the facade feature `dom` minimally (re-export, off by default, -D warnings + udeps clean). The WriteDom/grammar move stays wave 5+. Workspace green both modes.

Report (banked + final): search client design as landed, the chunked+lazy demo mechanics, the hydration-loss warning mechanism, upstreaming step scope, suite counts (workspace, view-dom-macro, demo-app build + all smokes + budgets), coordination friction, deviations.

---

Execute the combined Wave-4 contract+platform slice in /Users/mpl4/Desktop/workspace/OSS/topcoat. BANKING: spike/rustc-codegen-js/contract/parity/wave4-contract-notes.md from your FIRST action. You own: contract/ (all), spike/rustc-codegen-js/jsc-build/ (one targeted fix), demo-app/dashboard groundwork is NOT yours (wave 5). READ ONLY elsewhere; you may run the already-built demo-app binary for captures (mind the capture guard: no dev server up); `cargo test -p jsc-build` allowed. No git commit. Skill "style" for Rust. Read the wave-3 banks + this wave's backend/framework banks as they grow.

## Items

1. **The fake chart lib + js_extern vectors (M, FIRST — the backend agent polls for it).** Under contract/fixtures/js-extern/: a small vendored trace-recording fake of a chart library (ESM: default-or-named `Chart` constructor (new shape), instance.update(data) (send), instance.data getter (get), a method returning null sometimes (nullable), an indexed collection) — the shape the dashboard will use, written like harness/trace.mjs's recorder style (records constructor/method/getter calls with normalized args). Plus expected call vectors for the four shapes (what the backend's emission should produce when driving the fake — the trace form). Bank "CHART-FIXTURE READY". Also: a short JS-EXTERN.md contract page (the descriptor's semantics per shape; the backend agent designs the encoding — leave an encoding section stub they fill via your next check or note it as their CONTRACT.md's).
2. **jsc-build staleness fix (S).** Wave 3 found jsc-build only checks the view-dom-macro dylib EXISTS, not that it's current — every client expansion ran a stale macro for hours. Fix: preconditions compare the dylib mtime against the newest source mtime under view-dom/ + view-dom-macro/ + view-abi/ (or a content stamp like build_sysroot's — pick the robust one consistent with jsc-build's existing stamp patterns), with a clear Unmet/Fix naming the rebuild command. Tests. (You own jsc-build for exactly this fix; coordinate with nobody — no other agent touches it this wave.)
3. **Search parity fixture goes live (M).** Poll build/logs/wave4-framework-report.md for "SEARCH ISLAND SERVING"; then capture, de-pend, run: the full loop (typed input through delegated listeners, debounce window asserted, stubbed fetch honoring the wire vectors, list update, error negative) + hydration checks + the third-control pattern from nested. Attribute any mismatch precisely.
4. **Per-chunk budgets (S).** Once the framework's chunked demo lands (their bank): record per-chunk gzip baselines (counter/nested/search/shared/runtime/loader) with bands + containsIslands attribution updated; the coverage check that flags un-budgeted chunks.
5. **Drift + docs**: run-all.mjs --check zero; READMEs current.

Report (banked + final): the fake chart lib's surface + vectors, the staleness mechanism chosen, search fixture live results, budget table, deviations.
