defmodule CatalystFeatures do
  @moduledoc """
  Optional capabilities distributed with Catalyst.

  The modules in this application are ordinary compiled Elixir so they retain
  compile-time checks and Dialyzer coverage. Immutable installers in
  `priv/builtins/` contribute them through the same `Catalyst.ExtensionAPI`
  used by user extensions.
  """
end
